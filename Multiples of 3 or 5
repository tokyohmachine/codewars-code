def solution(number):
    total_sum = 0
    
    for n in range(1, number ):
        if n % 3 == 0 or n % 5 == 0:
            total_sum += n
        elif number < 0:
            return 0
    
    return total_sum
